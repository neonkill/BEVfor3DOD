# @package _global_
defaults:
  - override /model: only_road_model
  - override /data: nuscenes
  - override /loss: only_road_loss
  - override /metrics: only_road_metrics


experiment:
  name: only_road
    
loader:
  batch_size: 4

optimizer:
  lr: 4e-3

trainer:
  max_steps: 61530  #! batch_size 2: 123060